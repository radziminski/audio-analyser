import React from 'react';
import { Container, LogoIcon, LogoText } from './parts';

const Logo: React.FC = () => {
  return (
    <Container>
      <LogoIcon>A</LogoIcon>
      <LogoText>Analyser</LogoText>
    </Container>
  );
};

export default Logo;
